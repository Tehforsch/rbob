use std::{collections::HashMap, path::Path};
fn plot(
    pic_folder: &Path,
    plot_folder: &Path,
    data_folder: &Path,
    plot_template: &str,
    kwargs: HashMap<String, String>,
) {
    dbg!(pic_folder);
    dbg!(plot_folder);
    dbg!(data_folder);
    dbg!(plot_template);
}
